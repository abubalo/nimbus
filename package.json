{
  "name": "nimbus",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "http": "^0.0.1-security"
  },
  "scripts": {
    "start": "ts-node-dev --respawn --transpile-only --ignore-watch node_modules src"
  },
  "devDependencies": {
    "@types/node": "^20.3.2"
  }
}
